apiVersion: v1
kind: Service
metadata:
  name: stellarclassic-pulse
  labels:
    app: stellarclassic-pulse
spec:
  type: ClusterIP
  selector:
    app: stellarclassic-pulse
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
