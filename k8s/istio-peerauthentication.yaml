---
apiVersion: security.istio.io/v1beta1
kind: PeerAuthentication
metadata:
  name: stellarclassic-pulse-mtls
  namespace: default
  labels:
    app: stellarclassic-pulse
    component: service-mesh
spec:
  selector:
    matchLabels:
      app: stellarclassic-pulse
  mtls:
    mode: STRICT  # Enforce mTLS for all traffic
  portLevelMtls:
    8080:
      mode: STRICT
    50051:  # gRPC port
      mode: STRICT

---
apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: stellarclassic-pulse-authz
  namespace: default
  labels:
    app: stellarclassic-pulse
    component: service-mesh
spec:
  selector:
    matchLabels:
      app: stellarclassic-pulse
  action: ALLOW
  rules:
  # Allow health checks from anywhere
  - to:
    - operation:
        paths:
        - /health
        - /ready
        - /metrics
  
  # Allow API access with valid JWT
  - to:
    - operation:
        paths:
        - /api/*
        - /graphql
    when:
    - key: request.auth.claims[iss]
      values:
      - "https://auth.stellarclassic-pulse.example.com"
  
  # Allow internal service-to-service communication
  - from:
    - source:
        principals:
        - "cluster.local/ns/default/sa/stellarclassic-pulse"
        - "cluster.local/ns/monitoring/sa/prometheus"
    to:
    - operation:
        methods:
        - GET
        - POST
        - PUT
        - DELETE
