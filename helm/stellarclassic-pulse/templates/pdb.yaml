{{- if .Values.podDisruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "stellarclassic-pulse.fullname" . }}-pdb
  labels:
    {{- include "stellarclassic-pulse.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels:
      {{- include "stellarclassic-pulse.selectorLabels" . | nindent 6 }}
{{- end }}
