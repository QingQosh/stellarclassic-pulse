apiVersion: v1
kind: Service
metadata:
  name: {{ include "stellarclassic-pulse.fullname" . }}
  labels:
    {{- include "stellarclassic-pulse.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "stellarclassic-pulse.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
